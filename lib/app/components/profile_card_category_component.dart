import 'package:flutter/material.dart';

class ProfileCardCategoryComponent extends StatelessWidget {
  const ProfileCardCategoryComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        
      ),
    );
  }
}